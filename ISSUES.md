# FretAI 问题排查记录

> 面试复习专用：记录"遇到了什么意外现象、怎么一步步查到根因"，而不是"选了什么技术方案"。
> [DECISIONS.md](DECISIONS.md) 追踪技术选型权衡（为什么选 A 不选 B），这个文件追踪排查过程（现象 → 假设 → 排查手段 → 根因）。
> [PROGRESS.md](PROGRESS.md) 追踪进度。三个文件一起提交进 git，跨会话可查。
> 按时间顺序排列。

---

## 1. Key 检测：自己实现 Krumhansl-Schmuckler 算法，并用合成测试音验证代码与算法局限的边界

**背景**：librosa 没有内置"直接返回调性"的函数，只提供 chroma 特征提取（`librosa.feature.chroma_cqt`）。调性判断需要自己实现：算出整首歌 12 个音高类别的平均强度分布，再跟 24 种调（12 大调 + 12 小调）的 Krumhansl-Schmuckler 理论分布模板做相关系数比较，取最相关的作为结果。

**踩坑与排查过程**：真实歌曲（Thriller，公开资料显示是 F minor）测试时，检测结果是 `C# major`，跟预期差得较远。没有直接假设"代码写错了"就去改逻辑，而是先做了一次**交叉验证**：用 numpy 生成一段纯净的 C 大三和弦正弦波（C+E+G 三个频率叠加），跑同一套 `detect_key` 函数，结果正确识别为 `C major`（置信度 0.77，比 Thriller 那次的 0.66 更高）。

**结论**：代码逻辑本身没有问题——干净信号下工作正常。真实歌曲检测偏差，更可能来自：
1. Krumhansl-Schmuckler 这类"整曲平均 chroma"算法本身的已知局限——真实混音（鼓、贝斯、失真音色的谐波、多层编曲）会引入大量非调性噪声，不像纯正弦波那样"干净"；
2. Thriller 编曲本身较复杂（融合 disco/funk 元素），可能存在转调或调式游移，而"整首歌取一个平均值"的做法会抹掉这些细节。

**尝试过的算法改进（效果有限，如实记录）**：怀疑前奏/尾奏的和声不明确段落会稀释 chroma 分布，于是尝试只用歌曲中段 60%（掐头去尾各 20%）计算调性。结果：Thriller 的检测结果依然是 `C# major`，置信度几乎没变（0.662 → 0.677）。说明问题不在"边缘噪声"，而是整首歌（包括中段）的音高分布本身就更接近 `C# major` 模板——这进一步印证是算法在复杂编曲上的系统性局限，不是可以靠简单裁剪修复的小问题。**决定不再在算法层面继续调优**（比如换更复杂的 HPCP 特征、调 chroma 提取参数），转而在产品层面通过置信度展示 + 候选项 + 用户手动修正来应对这个不确定性（对应产品文档 §14 的设计），符合 MVP"先跑通、不过度优化不确定收益的方向"的原则。

**为什么这次排查方式值得记录**：这是一次"单元测试思维"在数据科学/信号处理场景下的应用——通过构造一个已知正确答案的、极简的合成输入，把"代码逻辑对不对"和"算法在复杂真实数据上表现好不好"这两个问题分离验证，而不是在复杂真实数据上直接改代码猜测调试。

**已知局限（诚实记录，供后续或面试提及）**：当前 Key 检测在复杂编曲/多层混音的歌曲上准确率有限；置信度计算是自行设计的"相对领先程度"近似指标，不是 Krumhansl-Schmuckler 算法本身自带的标准置信度定义。

**面试话术**：这条适合讲"如何验证一个不确定输出是否正确"——用可控的合成测试数据做单元验证，把"实现是否正确"和"方法本身的局限性"分开讨论，而不是遇到不符合预期的结果就直接怀疑并修改代码。

---

## 2. 和弦识别：沿用 Key 检测同一套验证方法论，确认低置信度是真实信号而非 bug

**实现思路**：跟 `key.py` 同一套技术路线，粒度不同——`key.py` 对整首歌算一次 chroma，`chords.py` 用 `beat_times`（`bpm.py` 产出）把整首歌按拍子切成一段段（每两拍一段，更贴近和弦通常按拍换的音乐规律），每段单独算 chroma，跟 24 种和弦模板（12 大三和弦 + 12 小三和弦，用点积而非相关系数比较——因为和弦模板是稀疏的 0/1 向量，点积可以直接理解成"这几个音的强度之和"）做匹配，取最相关的作为该段和弦，相邻同和弦的段落自动合并。

**踩坑与排查**：Thriller 上的识别结果，置信度普遍只有 0.27-0.31 且高度雷同（不同段之间几乎没有区分度），这个"扁平"的分布看起来像是置信度计算公式本身有问题，而不是单纯"和弦难识别"。

**验证方法（复用 Key 检测的方法论）**：合成一段"前 4 秒 C 大三和弦 + 后 4 秒 G 大三和弦"的纯净测试音频，手动构造均匀的 `beat_times`，跑 `recognize_chords`。结果：精确识别出 0.00-4.00s 是 `C`、4.00-8.00s 是 `G`，切换点准确，且置信度达到 0.85（远高于 Thriller 的 0.27-0.31）。

**结论**：置信度计算公式本身没有问题——干净信号下能正确输出高置信度、有区分度的结果。Thriller 上的扁平低置信度，是真实反映了复杂混音（人声、鼓、贝斯、多层编曲）下和弦确实难以精确判断这个客观现实，不是代码缺陷。

**一个有趣的副产品**：合成测试中 `C` 和弦的候选项第二名是 `Am`——C 大三和弦（C-E-G）和 A 小三和弦（A-C-E）共享两个音，这正好印证了产品文档 §6.11（扒带训练模式）里提到的"C 和 Am 因为共同音多而容易听混"这个乐理现象，算法在没有专门针对这一点设计的情况下自然涌现出了这个符合乐理直觉的候选排序。

**已知局限**：①"每两拍切一段"是简化假设，不是所有歌都严格按两拍换和弦；② 只覆盖大三和弦、小三和弦两种类型，没有七和弦、挂留和弦等更复杂的和声（符合 MVP"先覆盖最常见情况"的取舍）；③ librosa 内部 CQT 计算在片段边界会有 `n_fft too large` 的警告，这是库内部实现细节（对输入做更细粒度的内部窗口切分），不影响最终结果正确性，可以先不处理。

**面试话术**：可以和 Key 检测那次一起讲，形成一个完整的方法论闭环——"每次遇到和预期不符的输出，先用合成的、已知正确答案的极简数据做单元验证，区分是实现 bug 还是方法/数据本身的局限，再决定要不要继续投入优化"。

---

## 3. `prefer_position` 参数第一版测试"通过"，但测试用例本身有缺陷，功能实际没生效

**背景**：准备实现 Fingering Agent（让用户能说"5 品附近，不要大跳"来调整指法）之前，发现 `to_power_chord` 的 `prefer_position` 参数一直是"收下了但函数体内没用上"的状态（这个从骨架阶段就存在的问题，此前在 Guitar Arrangement Agent 那次也被记录过）。决定先把这个参数真正实现，再做依赖它的 Fingering Agent——避免做出"看起来支持但实际不生效"的假功能。

**第一版实现**：给根音的品格算出"加减一个八度（12 个半音）"的候选集合，在 `0-15` 品的合法范围内，选离用户指定的 `prefer_position` 最近的一个。

**踩的坑**：第一次验证选的测试用例是 `D` 和弦，分别测 `prefer_position=5`、`prefer_position=0`、不指定三种情况——结果三次输出**完全一样**（都是 `position: 10`）。表面上看像是"功能没生效"，但没有立刻断定是 bug，而是先验证"这几个测试用例本身是否有能力检测出功能生效与否"。

**排查过程**：手动算了一下 `D` 的候选集——`base_fret=10`，加减 12 后是 `22` 和 `-2`，`-2` 是负数不合法，`22` 超出了 `0-15` 的范围也不合法，所以**候选集里其实只有 `10` 这一个选项**。不管 `prefer_position` 传几，`min()` 在只有一个候选时永远只能返回这一个——**这不是 `prefer_position` 没生效，而是这三个测试用例根本没有测试能力**，无法区分"功能正常工作"和"功能完全没接线"这两种情况。真正的根因是：`0-15` 品这个"合法范围"设得太窄，导致绝大多数根音在"加减一个八度"后，只有 1 个候选落在这个范围内，`prefer_position` 的选择空间形同虚设。

**修复**：把合法范围从 `0-15` 扩大到 `0-20`（更贴近真实吉他指板至少 20-22 品的实际情况），让更多根音能有 2 个真正可选的候选品格。用新的测试用例（`C` 和弦，`base_fret=8`，加 12 后是 `20`，正好落在新范围内）重新验证：`prefer_position=5` 时选 `position:8`，`prefer_position=18` 时选 `position:20`——两次结果终于不一样了，证明参数真正生效。

**为什么这次排查值得记录**：这是一个"测试看起来通过了，但测试本身没有意义"的典型案例——三次调用不报错、返回值格式都对，很容易被误判成"验证通过"，但实际上只是恰好选了一个无法区分"生效"和"不生效"的输入。教训是：验证一个"可选参数会不会影响结果"的功能时，必须先确认"不同的参数值，理论上是否真的可能导致不同的输出路径"，而不是只看"跑起来有没有报错"。

**面试话术**：适合讲"如何识别一个"通过"的测试其实没有测试效力"——表面现象是三次输出一致（看起来像 bug，也可能像是恰好设计成这样），关键是没有停在"报告结果不一致/一致"，而是往上一层追问"这几个输入案例本身，有没有能力区分好坏"，最终定位到真正的根因是候选范围设计过窄。

---

## 4. Fingering Agent 端到端测试暴露：问题 #3 的修复不彻底，`prefer_position` 只是"部分生效"

**背景**：#3 把 `to_power_chord` 候选范围从 `0-15` 扩到 `0-20` 之后，用单个和弦（C）验证过参数确实能生效，当时判断问题解决了。这次实现 Fingering Agent 后，用产品文档 §9.2 的真实场景（`F#m-D-A-E`，要求"5 品附近"）做端到端测试，才暴露出 #3 的修复其实只是"缓解"，没有根治。

**现象**：LLM 正确解析出 `prefer_position=5`（先单独验证过这一层没问题，隔离出问题在 `to_power_chord`），但四个和弦里只有 `F#m`（2 品）和 `A`（5 品）落在合理区间，`D` 停在 10 品、`E` 停在 0 品，完全不在"5 品附近"。

**排查过程**：没有直接怀疑"候选范围还不够大就再调个数字"，而是对四个根音逐一手算候选集：
- `F#`: `[2, 14]` → 选 2，正确
- `D`: `base_fret=10`，减 12 是 -2（不合法），加 12 是 22（仍超出 0-20）→ **候选集里只有 `[10]` 一个选项，无从选择**
- `A`: `[5, 17]` → 选 5，正确
- `E`: `base_fret=0`，候选 `[0, 12]` → 离 5 更近的是 0，但 0 和 12 都谈不上"在 5 品附近"，只是矬子里拔将军

**根因**：#3 的修复（扩大范围到 0-20）只是让*部分*根音多了一个可选的候选，但没有解决问题的本质——"同一个根音在指板上，只能通过整八度（12 半音）跳动来换位置"这个枚举方式，天生就覆盖不了"任意目标品格附近都有合理选项"这个诉求。真正需要的是一个不同维度的能力：**5 弦根音的 power chord 形状**（跟 6 弦根音是同一个音，但落在指板上完全不同的位置，能让可选候选真正变得密集），而不是在"同一个 6 弦根音形状的不同八度"之间来回选。

**为什么先不修**：完整方案（实现 5 弦根音形状 + 在 6 弦/5 弦两套形状之间也做选择）工作量不小，属于产品文档 §6.8 把位优化 Agent 的完整范畴，跟 Fingering Agent 当前"能诚实处理用户请求、不做假功能"的验收标准是两回事——Fingering Agent 本身的架构（LLM 解析请求 -> 调用 `to_power_chord`）是对的，问题在于它调用的底层工具能力还不够强。

**这次记录跟 #3 的关系**：#3 处理的是"测试用例选得不好，看不出问题"；这次是"测试用例选对了（用了真实场景的完整案例），发现之前的修复只是解决了单个案例，没有解决系统性问题"。两次是同一条根因链路的延续排查，说明"用真实、完整的端到端场景做验证"比"用单个孤立案例验证"更容易暴露系统性设计局限。

**面试话术**：这条适合讲"一次局部修复之后，怎么判断它是不是真的根治了问题"——#3 当时用一个案例验证通过，容易让人误以为问题解决了；只有换成产品真实使用场景的完整测试，才暴露出之前的修复只是"部分覆盖"。这也说明为什么端到端测试和单元测试都需要，缺一个都可能漏掉某类问题。

---

## 5. Editor Agent 端到端测试暴露：大批量和弦场景下 LLM "偷懒工具调用"，静默产出全空结果

**背景**：里程碑 6 把 `chat.py` 端点接到 Editor Agent 的 `apply_edit` 后，用真实歌曲（176 个和弦、已经是 `electric_power_chord` 类型的编配）做端到端测试，发指令"改成5品附近的power chord"。

**现象**：给 `runner.py` 加了执行轨迹记录（`AgentTrace`，记录每一轮 LLM 是否调用工具、调用了什么、工具返回了什么）之后才看清楚——`trace.steps` 只有一个 `role: "final"` 的 step，**LLM 全程没有发起过任何一次 `to_power_chord` 工具调用**。它直接在最终 JSON 的 `new_chords` 字段里把 176 个和弦名原样抄了一遍（`C→C`、`Cm→Cm`……和输入完全一样），却在 `explanation` 字段里编造了一段"前5个和弦转换示例：C→C5(5品), Cm→C5m(3品)..."的话——这些具体品格数字并未经过真实工具调用验证，是模型编出来的。

**这暴露的下游问题**：`editor.py` 的 `_build_arranged_chord` 完全信任 LLM 返回的 `new_chords` 列表，逻辑是"和弦名以 `5` 结尾才当作 power chord 处理、调用 `to_power_chord` 查真实指法，否则指法留空"。因为 LLM 返回的和弦名根本没变（不以 `5` 结尾），所以最终返回的 `arrangement.chords` 里每一条 `fingering` 都是空字符串、`position` 都是 0——一个表面看起来"正常返回、没报错"的结果，实际上完全没有执行用户要求的操作。

**判断该问题的性质**：不是规则系统或 `runner.py` 循环机制的 bug（`to_power_chord` 工具本身、Tool Use 循环逻辑都是对的，如果模型愿意调用，循环会正确处理）。是模型在任务量很大（system prompt 里说"需要对每个和弦分别调用"，意味着 176 次重复工具调用）时，选择"用语言假装完成任务"而非老实执行——这类现象业界有时称为 **lazy tool use**，本质上和幻觉（hallucination）同源：输出看起来合理、有细节，但没有真实依据。

**排查方式（进行中）**：为了做"控制变量"隔离测试，绕开数据库和 HTTP 层，直接调用 `apply_edit`，构造一个只有 4 个和弦的短编配（`backend/tests/scratch_editor_short_progression.py`），测试同样的"改成 power chord"指令在小规模输入下是否会正常触发工具调用。目的是区分：①是否单纯因为和弦数量太多，模型才走捷径；②还是"编配已经是 power chord 格式"这个语义本身就让模型觉得无需再调用工具。

**这次发现的价值**：印证了 PROGRESS.md 里程碑 9 "安全与鲁棒性" 提到的方向——结构化输出的可靠性不能假设"模型自觉"。当前系统完全没有校验机制去检测"模型返回的结果是否真的经过了工具调用"，这是一个需要在生产化之前解决的真实风险点，不只是这一次演示数据凑巧触发的偶然现象。

**面试话术**：这条适合讲"Agent 系统里一种容易被忽视的失败模式"——不是报错、不是格式错误，而是模型在高重复度任务上选择"敷衍"，输出的置信文本反而会掩盖这个问题（如果没有加 trace 记录，光看最终 JSON 格式完全正确，很容易误判为"功能正常"）。这也是为什么要给 Agent 循环加可观测性（trace）——没有它，这类问题几乎不可能被发现。

**排查过程中修正的认识（重要）**：最初的排查方向是"验证 LLM 偷懒是不是跟和弦数量有关"，隐含假设是"如果模型老实工作，逐个调用 176 次工具就是正确路径"。但进一步推演发现这个假设本身站不住：`runner.py` 的 Tool Use 循环里，每一次工具调用后都要重新发起一次完整的 API 请求（把工具结果塞进 `messages`，再问模型下一步做什么），176 个和弦如果真的逐个调用，意味着 176 次网络往返，而且 `messages` 是追加式增长、不做裁剪（呼应里程碑 9 "上下文管理"待办），每次请求体都会变长。就算模型完全配合，这也会是几分钟量级的响应时间，用户体验上不可接受。

**这说明了什么**：模型这次"编造结果"的行为不能只归咎于"模型不老实"——某种程度上，它是在一个**任务形式本身不合理**的场景下做出的（虽然方式错误、但方向上可以理解的）应激反应。真正的根因不只是"结构化输出缺乏校验"，更前置的是"逐个和弦调用工具"这个交互设计，从一开始就不该被用在大批量场景上。这是一次排查过程本身改变了问题定义的例子：一开始想验证"是不是数量太多导致模型偷懒"，验证方式还没跑，就已经从"数量是否是诱因"变成了"当前架构在数量大的时候本身就不可行，不管模型听不听话都要改"。

**可能的修复方向（第一轮，提出后马上被下一步验证推翻了部分假设，过程照实保留）**：
1. **不再让 LLM 逐个调用工具**：把"改成 power chord"这类可以批量套用同一规则的操作，改成一次工具调用处理整个列表（比如 `to_power_chord_batch(chords, prefer_position)` 返回列表），而不是让 LLM 承担"对每个元素都调用一次"这种机械重复劳动——这类任务本来就该由确定性代码做批量处理，回到 [DECISIONS.md](DECISIONS.md) #6 的架构原则："规则系统负责算事实，Agent 负责决策+解释"，逐个转换和弦是事实计算，不需要 LLM 参与循环。
2. **如果确实需要 LLM 逐条判断**（比如每个和弦要不要转、转成什么需要语境判断），才考虑分批处理 + 加校验机制（比对返回和弦数与工具调用次数）。
3. 两个方向不互斥：先判断这个场景（转 power chord）是否真的需要 LLM 逐条介入，大概率不需要，可以直接归入方向 1。

**实现并测试方向 1**：新增 `to_power_chord_batch(chords, prefer_position)`（`power_chord.py`），复用现有的单个 `to_power_chord`，仅做列表包装。`editor.py` 的工具集从"单个 `to_power_chord`"换成"批量 `to_power_chord_batch`"，system prompt 和工具 description 都明确写"一次调用处理整个列表，不要逐个调用"。同时理清了一个衍生问题：原来的 `_build_arranged_chord` 靠"和弦名是否以 `5` 结尾"这种字符串判断反推指法，等于把工具真实返回值扔掉、组装阶段又重新调用一次 `to_power_chord`——改成直接从 `trace.steps` 里取出 `to_power_chord_batch` 那次调用的真实 `arguments`/`result`，用 `zip` 建立"原始和弦名 → 转换结果"的映射表，组装时优先查这个映射表，不再依赖 LLM 转述的和弦名字符串反推。

**验证结果，推翻了排查方向的前提假设**：用 `backend/tests/scratch_editor_short_progression.py`（4 个和弦：F#m-D-A-E，指令"改成power chord"）重新测试，**LLM 依然一次工具都没调用**（`trace.steps` 只有一个 `role: "final"`），只是直接在文本层面把每个和弦名拼接了个"5"后缀（`F#m`→`F#m5`、`D`→`D5`），`fingering`/`position` 全部是空值/0，和批量工具毫无关系——`to_power_chord_batch` 从未被触发。

**这次验证的意义**：最初的假设是"176 个和弦太多，模型走捷径"，隐含着"如果给的和弦少一点，模型就会老实调用工具"。但这次用只有 4 个和弦的输入，模型依然选择不调用工具——说明**根因不是任务量大小，而是模型判断"改成 power chord"这类任务凭自己的乐理知识就能直接完成文本拼接，不认为有调用工具的必要**。这是一次典型的"排查方向被下一步实验数据推翻，需要往回修正假设"的过程：批量工具本身的设计（把 176 次调用压缩成 1 次）仍然是对的、该保留（它解决了"就算模型配合，架构也跑不动"这个独立问题），但它不能单独解决"模型不调用工具"这件事——因为问题不在"调用成本太高"，而在"模型判断不需要调用"。

**下一步排查方向**：需要转向研究"如何让模型在这类任务上愿意/被强制调用工具"，而不是继续在"怎么把工具设计得更省调用次数"上打转。候选方向：①用 `tool_choice` 强制模型必须调用指定工具（不给它"自己回答"的选项），OpenAI 兼容接口通常支持 `tool_choice={"type": "function", "function": {"name": "..."}}` 这种强制指定；②如果强制调用后模型行为依然不可靠，考虑彻底不依赖 LLM 做这类"纯规则套用"的判断——直接在代码层面用关键词/规则识别"改成 power chord"这类明确意图，跳过 LLM 决策这一步，只在真正需要语义理解的场景（比如"5 品附近，不要大跳"这种带模糊修饰的请求）才交给 LLM。

**实现并测试方向①（tool_choice 强制调用）**：给 `runner.py` 的 `run_agent_with_tools` 加 `force_tool_use: bool` 参数——为 True 时第一轮请求传 `tool_choice="required"`（模型必须调用某个工具，但不锁定具体是哪个，因为 `apply_edit` 有 `transpose_progression`/`to_power_chord_batch` 两个工具，指令决定该用哪个），后续轮次改回 `"auto"`（否则模型永远无法结束循环、正常给出最终答案）。`editor.py` 调用处设 `force_tool_use=True`。

**踩坑 1：thinking mode 与 tool_choice 冲突**：一上来直接跑就报错——`InternalError.Algo.InvalidParameter: The tool_choice parameter does not support being set to required or object in thinking mode`。查阿里云文档确认：qwen3.5-flash 是混合推理模型，默认开启"thinking mode"（先生成内部推理过程再给最终回复），DashScope 明确不支持在 thinking mode 下强制 `tool_choice`（推测因为"自由思考决定要不要用工具"和"外部强制必须用"这两种机制设计上冲突）。修复：`enable_thinking` 不是 OpenAI 标准参数，openai SDK 不认识这个字段，需要通过 `extra_body={"enable_thinking": False}` 透传给 DashScope，`force_tool_use=True` 时一并关闭 thinking mode。

**验证 tool_choice 是否真的生效——先隔离出一个最小复现场景**：不再直接在 `apply_edit`/`editor.py` 这层堆叠测试（担心多个变量混在一起分不清是谁的问题），单独写 `backend/tests/scratch_tool_choice_required.py`，只传最基本的 `messages` + 单个工具 + `tool_choice="required"` + `extra_body`，不经过 `runner.py`/`editor.py` 封装。**结果：生效了**——`finish_reason: tool_calls`，模型老实调用了 `to_power_chord_batch`，参数正确。这证明 `tool_choice="required"` 机制本身没问题，问题出在 `editor.py`/`runner.py` 的某个额外因素上。

**踩坑 2：`response_format=json_object` 要求 messages 里必须出现小写 "json"**：把最小复现场景的 `response_format={"type": "json_object"}` 也加上（对齐 `editor.py` 的真实调用参数）后重新测试，直接报错：`'messages' must contain the word 'json' in some form, to use 'response_format' of type 'json_object'`。奇怪的是 `editor.py` 的 `SYSTEM_PROMPT` 里明明写了"JSON"（大写），但报错依然出现——推测这条校验是大小写敏感的，只认小写 "json"。这也解释了一个疑点：为什么之前（没有 `tool_choice="required"` 时）同样传了这个 `response_format`，却从未报过这个错——大概率这条校验只在配合 `tool_choice="required"` 或某些特定路径下才被触发检查，"没报错"不等于"这个约束不存在"，只是运气好没撞上触发条件。把 system prompt 里加一句包含小写 "json" 的话之后，这个报错消失。

**踩坑 3（更本质）：`tool_choice="required"` 与 `response_format=json_object` 同时生效时会互相冲突**：解决踩坑 2 后重新测试，发现请求虽然不再报错，但 `finish_reason: stop`、`tool_calls: None`——模型直接跳过工具调用，给出了一段格式正确的 JSON 文本作为最终答案。也就是说，"必须调用工具"和"必须输出 JSON 格式内容"这两个约束**同时生效时，后者的优先级压过了前者**，模型选择直接满足"给出 JSON"这个要求，而不是先调用工具。这是 [DECISIONS.md](DECISIONS.md) #12 提到的"两个独立验证过的机制，组合起来是否还稳定"这类风险的又一次真实命中——上次是 `response_format` 单独 + `tools`（可选调用）组合没问题，这次是 `response_format` + `tool_choice="required"`（强制调用）组合出问题，说明"强制"这个额外约束改变了组合的稳定性，不能简单认为"两两分别验证过就可以随意叠加"。

**这次排查的完整链路**：任务设计不合理（逐个调用工具跑不动，见上文）→ 批量工具是对的方向 → 但批量工具本身也没被调用（模型判断不需要）→ 尝试用 `tool_choice="required"` 强制 → 牵出 thinking mode 冲突 → 解决后用最小场景验证机制本身没问题 → 加回 `response_format` 复现出 `messages` 缺 "json" 关键词的报错 → 解决后发现 `tool_choice="required"` 和 `response_format` 两个约束本身互相冲突，"强制工具"被"强制 JSON"盖过。每一层修复都会暴露下一层新问题，是排查过程"层层剥开"的典型例子。

**实现并测试方案①（分两次请求）**：`runner.py` 的 `force_tool_use=True` 时，第一轮请求只传 `tool_choice="required"`、不传 `response_format`；模型必须调用工具后，进入生成最终答案的那一轮，才把 `response_format` 带上（此时已经不需要再强制工具，模型该收尾了）。两个有冲突的约束不再同时出现在同一次请求里。`editor.py` 的 `SYSTEM_PROMPT` 也顺手把"JSON"改成小写"json"（呼应踩坑 2）。

**验证：短和弦场景通过**：`backend/tests/scratch_editor_short_progression.py`（4 个和弦）重新测试，`trace.steps` 里终于出现了 `role=tool_call`，`to_power_chord_batch` 被正确调用、传入完整和弦列表、返回真实指法数据。

**暴露了组装逻辑的独立 bug**：工具调用成功了，但最终 `new_arrangement.chords` 里 `fingering`/`position` 依然是空的。排查发现 `editor.py` 组装结果那段代码有个"用错 key"的映射错误——用**转换前**的和弦名（如 `"F#m"`）作为字典 key 存工具结果，却拿 LLM 最终 JSON 里**转换后**的和弦名（如 `"F#5"`）去查这个字典，两者对不上，永远查不到，全部落进"找不到、留空"的兜底分支。这段代码此前从未被"工具真的被调用"这条路径执行过（因为在此之前模型从来不调用工具），是一段"写完了但从没跑通过"的代码，直到这次修复才第一次被真正执行到。**修复**：不再用和弦名字符串反查字典，改成直接用 `zip(tool_call.arguments["chords"], tool_call.result)` 按调用时的原始顺序配对——这是 API 调用本身保证的对应关系，不依赖字符串匹配或"模型是否保持顺序"的假设。修复后短和弦场景验证通过：`F#m`→2品`2-4-4xxx`、`D`→10品`10-12-12xxx`、`A`→5品`5-7-7xxx`、`E`→0品`022xxx`，与产品文档 §7.1 的验证案例数值完全吻合。

**验证：176 个和弦真实场景暴露参数类型问题**：重新用真实歌曲的 176 和弦编配、指令"改成5品附近的power chord"跑通端到端 chat 接口，触发新的 500 错误：`TypeError: unsupported operand type(s) for -: 'int' and 'str'`，定位到 `power_chord.py` 的 `abs(f - prefer_position)`。根因：LLM 从"5品"里提取出的数字被当成字符串 `"5"` 传给了 `prefer_position` 参数，尽管 JSON Schema 里声明的类型是 `integer`——**schema 只是给模型的提示，不是强制类型校验**，模型仍可能传回不符合声明类型的值。这跟 PROGRESS.md 里程碑 9 "工具调用参数校验"那条待办完全对应，短和弦测试没暴露这个问题是因为那次调用没有传 `prefer_position`（指令是"改成power chord"，不带具体品格数字）。

**修复：在系统边界做类型容错**：`to_power_chord` 入口加一层转换——`prefer_position` 不是整数时尝试 `int()` 转换，转换失败（比如传的是"低把位"这种无法解析的文字）就当作未提供（`None`），走"不指定偏好品格"的默认路径，而不是让异常直接导致整个请求 500。这是一个**有意选择的静默降级**：`prefer_position` 是"锦上添花"参数，缺了它 `to_power_chord` 仍能算出合法结果，只是不会精准匹配到用户要求的品格附近；相比"重试"（多一次 API 往返、不保证第二次就对）或"报错"（用户什么都拿不到），静默降级能保证用户至少拿到一个可用的转换结果。**已知的可观测性缺口（有意先不处理）**：这个降级完全没有留痕——用户不会知道"5 品附近"这个要求其实没生效，LLM 的 `explanation` 文字大概率还会声称"已调整到 5 品附近"（因为它自己也不知道参数没生效）。这跟 #5 前面"模型编造结果"的性质类似，只是这次是参数丢失导致的被动结果，不是模型主动编的。如果以后要处理，可以考虑给降级路径加个警告标记、通过 trace 或日志暴露出来，但目前判断优先级不如"先让端到端流程跑通"，先记录在案。

**最终验证：176 个和弦真实场景端到端通过**：用真实歌曲编配（`electric_power_chord` 类型，176 个和弦）、指令"改成5品附近的power chord"完整跑通 `chat` 接口。确认三件事同时成立：①`trace.steps[0].role: "tool_call"`，模型一次性把全部 176 个和弦传给 `to_power_chord_batch`，没有逐个调用、也没有跳过工具直接编造；②`arguments.prefer_position` 实际值是字符串 `"5"`（再次印证"模型传字符串不是偶然，是常态行为"），但这次未再报错，`int()` 容错转换正常生效；③返回的 `arrangement.chords` 每一条都有真实指法/把位数据（如 `Gm→G5(3品)`、`F#m→F#5(2品)`），跟品格公式吻合，不再有任何空值。

**状态**：已解决。核心问题（强制工具调用与 JSON 输出组合冲突）、两个衍生 bug（组装逻辑用错 key、参数类型缺乏容错）均已修复并通过真实大规模场景验证。批量工具函数 `to_power_chord_batch` 证明是正确方向，予以保留。

**完整技术总结（面试用）**：这是一次跨越多个层次的排查——从"任务设计是否合理"（逐个调用工具在大批量下不可行）到"模型行为"（明知有工具却判断不需要调用）到"供应商特定限制"（thinking mode 与 tool_choice 冲突、response_format 的隐藏文本校验）到"机制组合稳定性"（两个分别验证过的约束一起用会冲突）到"系统边界防御"（工具调用参数类型不可信、需要容错）到"未被执行代码的隐藏 bug"（组装逻辑的 key 匹配错误从未被真正测试路径覆盖过）。每一层修复都会暴露下一层新问题，最终解法是多个独立改动的组合，不是单一的"一次性修好"。

---

## 排查记录写作习惯

以后每次遇到"现象和预期不符、需要排查根因"的情况（不是语法小错误，是系统层面的排查过程），都补一条到这里，格式保持一致：**背景 → 现象 → 排查过程 → 根因/结论 → （可选）已知局限 → 面试话术**。跟 [DECISIONS.md](DECISIONS.md)、[PROGRESS.md](PROGRESS.md) 一起提交进 git，三个文件分工不同：PROGRESS 看"做到哪了"，DECISIONS 看"为什么选这个方案"，这个文件看"怎么查出问题根因的"。
