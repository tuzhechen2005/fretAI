fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
pydantic-settings>=2.6
sqlalchemy>=2.0
aiosqlite>=0.20

# 音频分析
librosa>=0.10
numpy>=1.26
soundfile>=0.12

# LLM Agent（阿里云百炼 OpenAI 兼容接口）
openai>=1.0

# 导出（Markdown 拼装 + HTML 渲染成 PDF；实际选型是 weasyprint 不是最初设想的 reportlab，见 DECISIONS.md）
markdown>=3.6
weasyprint>=62.0
